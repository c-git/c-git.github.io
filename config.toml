# The URL the site will be built for
base_url = "https://c-git.github.io/"

# The site title and description; used in feeds by default.
title = "One's Notes"

# The site theme to use.
theme = "zola_easydocs_theme"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# For overriding the default output directory `public`, set it to another value (e.g.: "docs")
output_dir = "docs"

# When set to "true", a feed is automatically generated.
generate_feed = true

author = "One"

# Configuration of the Markdown rendering
[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether external links are to be opened in a new tab
# If this is true, a `rel="noopener"` will always automatically be added for security reasons
external_links_target_blank = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true

[link_checker]
# Skip link checking for external URLs that start with these prefixes
skip_prefixes = [
  "https://support.discord.com", # Client error status code (403 Forbidden) received
  "https://dev.mysql.com/", # Client error status code (403 Forbidden) received
]
# Skip anchor checking for external URLs that start with these prefixes
skip_anchor_prefixes = [
  "https://github.com/",
  "https://www.markdownguide.org/extended-syntax/",
  "https://rust-lang.github.io/rustfmt/", # Site uses vue and tags don't exist when fetched https://github.com/getzola/zola/issues/2351
  "https://discord.com",
]

[extra]
# Put all your custom variables here
logo = "notepad.png"
favicon = "notepad.png"
easydocs_logo_always_clickable = true
easydocs_uglyurls = false
easydocs_heading_threshold = 1
git_url = "https://github.com/c-git/c-git.github.io/"
